#include "main.h"
/**
 * _isupper - Entry point
 * @c: is to be checked
 * Return: 1 if successful, otherwise 0
 */

int _isupper(int c);
{
	int c;

	if (isupper(c))
	{
		return (1);
	}

	else

	{
		return (0);
	}
}
