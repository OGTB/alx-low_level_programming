#include "main.h"
/**
 * main - entry point
 * Description: check for lowerCase @c
 * @c: the character to check
 * Return: 1 if lowerCase and if otherwise
 */

int _islower(int c);
{

	if (c >= 'z' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	_putchar('\n')
}
